# Independently verified module evidence

The JSON files in this directory are generated by
`tests/verify-accepted-evidence-artifact.py` from the immutable aggregate
artifact of GitHub Actions run
[`29648435978`](https://github.com/Yunushan/linux-software-installer/actions/runs/29648435978).

They bind 90 `family/module` evidence keys to commit
`f59c5765b5e68ba4e074331a1b494a1bfdbcb125`, the aggregate index digest, and
the published artifact digest:

```
sha256:e9b7e22c95106d4e99fe8bff465e69c78afbb70b3025458fa4380dd5937bca64
```

The artifact was independently downloaded and verified for all 370 indexed
cells. These reports are verification inputs, **not** admissions: every
legacy row still requires an exact parity review, and service contracts still
require accepted disposable-VM/systemd attestations. Do not add a row to
`docs/accepted-evidence.tsv` or change its inventory disposition solely because
a report exists here.
